#include "Player.h"

Player::Player(int name, int hitPoint, int magicPoint, int attackPoint, int defencePoint)
{
	_name = name;
	_hitPoint = hitPoint;
	_magicPoint = magicPoint;
	_attackPoint = attackPoint;
	_defencePoint = defencePoint;
}
